void main(List<String> args) {
  List<int> numbers = [1, 2, 3, 4, 5, 6];
   int lastNumber = numbers.last;
  print(lastNumber);
  int number1 = numbers.elementAt(numbers.length - 1);
  print(number1);
}
